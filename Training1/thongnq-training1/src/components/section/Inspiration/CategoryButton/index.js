import React from "react";

import { Wrapper} from "./CategoryButton.styles";

const CategoryButton = ({children}) => (

    <Wrapper>
        {children}
    </Wrapper>
)

export default CategoryButton;